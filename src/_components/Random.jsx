import React from "react";


function Random({perguntas}){
  console.log(`Perguntas recebidas, ${perguntas}`);
    console.log(`Quantidade de objetos: ${perguntas.length}`);
    
    // Verifique no console do navegador se os dados aparecem
    // Se aparecer, o array está sendo passado corretamente!
    
    return <div>Verifique o console do navegador</div>;
}

export default Random;